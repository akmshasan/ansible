This playbook can be used to setting up elasticsearch cluster. Before running the playbook, the following changes must be done:
  - Update the playbook with appropriate server IP
  - Update inventory file
