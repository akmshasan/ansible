---
- name: Copy ENV service file to server
  ansible.builtin.template:
    src: vminsert.j2
    dest: /etc/default/vminsert
    owner: root
    group: root
    mode: '0644'

- name: Copy binary to server
  copy:
    src: vminsert-prod
    dest: /usr/local/bin
    mode: 0755
    owner: root
    group: root

